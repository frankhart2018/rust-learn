
fn main() {
    // Function-like macros define macros that look like function calls

    // These take a TokenStream parameter and their definition manipulates
    // the TokenStream using Rust code as the other two types of procedural
    // macros do

    // An example is:-
    // let sql = sql!(SELECT * FROM posts WHERE id=1);
}
                